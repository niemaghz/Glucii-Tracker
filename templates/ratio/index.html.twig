{% extends 'base.html.twig' %}

{% block title %}Hello RatioController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	{{ form_start(Ratio) }}
	{{ form_row(Ratio.glucide)}}
	{{ form_row(Ratio.glycemie)}}
	{{ form_row(Ratio.Calcul)}}
	{{ form_end(Ratio) }}

	<div>
		<h5>you have
			<br/>to take<strong>
				{{insuline}}</strong>
			units
			<br/>
			of insuline</h5>
	</div>
{% endblock %}
